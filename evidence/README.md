# Evidence

`requirements.sha256` identifies the preregistered contract used by the EC1
architecture tournament. `gates.toml` contains the machine-readable thresholds.

Future run directories are intentionally untracked until a result is complete.
Committed evidence must contain tool versions, commands, seeds, environment,
raw-result hashes, and the candidate or specification digest it evaluates.

